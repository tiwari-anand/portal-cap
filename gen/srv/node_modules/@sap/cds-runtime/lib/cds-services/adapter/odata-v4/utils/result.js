const getVirtualsFromResult = (target, result) => {
  const virtuals = {}
  Object.keys(target.elements)
    .filter(k => target.elements[k].virtual)
    .forEach(k => (virtuals[k] = result[k]))
  return virtuals
}

module.exports = {
  getVirtualsFromResult
}
